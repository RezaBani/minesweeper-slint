import { AboutDialog } from "about_dialog.slint";
import { MainWindow } from "main_window.slint";
import { Position, Tile, GameState, GameDifficulty } from "types.slint";
export { AboutDialog,MainWindow }
